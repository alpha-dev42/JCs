<!DOCTYPE html>
<html>
<% if alert %>
    <p id="alert" class="alert alert-danger"><%= alert %></p>
<% end %>
<% if notice %>
    <p id="notice" class="alert alert-info"><%= notice %></p>
<% end %>

 <head>
  <title>Jay's Cameras</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
  </head>

  <body> 
    <!-- Fixed navbar -->
    <nav class="navbar navbar-default navbar-fixed-top">
      <div class="container-fluid navbar-main">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#nav-collapse-connect" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/"><%= image_tag("JC_logo.png", class: "img-responsive logo") %>
          </a> <!--end projectName -->
        </div>
        <div class="collapse navbar-collapse" id="nav-collapse-connect"> <!--end navbar-header -->
          <ul class="nav navbar-nav">
            <li><a href="/">Home</a></li>
            <li><a href="/products">Products</a></li>
            <li><a href="/orders">Orders</a></li>
            <li><a href="/simple_pages/about">About</a></li>
            <li><a href="/simple_pages/contact">Contact</a></li>
          </ul>
          <ul class="nav navbar-nav navbar-right search-bar">  
            <!-- Search Bar -->
            <li>
              <% if user_signed_in? %>
                <%= link_to('Logout', destroy_user_session_path, method: :delete) %>
              <% else %>
                <%= link_to('Login', new_user_session_path) %>
              <% end %>
            </li>
          </ul> 
        </div> <!-- /.navbar-collapse -->
      </div> <!-- /.container-fluid -->   
  </nav>

<%= yield %>
<div class="anchor-bottom"></div>
  <footer class="site-footer">
    <div class="container">
      <!-- search form -->
      <div class="row">
        <div class="col-sm-6 text-center hidden-xs">
          <h3 class="searchform-header">Can't Find What You Are Looking For?</h3>
        </div><!-- /.col-sm-6 -->
        <div class="col-sm-6">
           <%= form_with(url: products_path, method: 'get', local: true) do |form| %>
          <div class="input-group row">
            <%= form.text_field :q, id: 'q', value: params[:q], class: "form-control", placeholder: "Search Here" %>
            <span class="input-group-btn">
            <%= form.submit 'Search', class: 'btn btn-success search-btn'%>
            </span>
          </div>  
        <% end %>
        </div>

        <div class="col-sm-12">
          <hr>
        </div>
      </div><!-- /.row -->
    <h4 class="text-center"><a class="footer=styling" href="/simple_pages/contact">&copy; Jay's Cameras 2018
    </a></h4>    
    </div><!-- /.container -->
  </footer>

</body>

</html>
